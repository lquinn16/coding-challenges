import * as React from "react";

export default function Header() {
    return (
        <div className="Header">
            <img className={'logo'} src={require('../assets/Stake_logo.png')} alt={'Stake logo'}/>
        </div>
    );
}
